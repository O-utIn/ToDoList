# “极简待办清单 APP”产品需求文档（PRD）

---

## 1. 文档概述

### 1.1 编写目的
本产品需求文档（PRD）旨在详细定义“极简待办清单 APP”的功能需求、界面逻辑、技术实现路径以及非功能性要求。本文档作为该移动互联网应用课程设计项目的指导性技术文件，用于规范后续的开发、测试与项目报告撰写。

### 1.2 项目背景
在快节奏的现代生活中，时间管理与习惯养成逐渐成为用户的刚性需求。本项目参考了主流市场上的“极简待办清单”类应用，旨在设计并实现一款界面清新、交互简单且功能实用的待办与习惯管理工具。该应用不仅满足日常任务记录需求，更融合了番茄工作法、本地数据存储、广播通信、后台服务、位置服务及轻量级本地推荐算法，用以检验和展示在《移动互联网应用》课程中所掌握的 Android 开发技术。

---

## 2. 产品定位与目标用户

*   **产品定位**：一款融合了“习惯打卡”、“待办清单（To-Do）”与“番茄专注（Pomodoro）”的极简风时间管理工具。
*   **目标用户**：学生群体、职场办公人员，以及需要克服拖延症、培养良好生活与工作习惯的效率追求者。
*   **设计原则**：
    *   **极简主义**：界面视觉留白得当，操作路径尽可能缩短。
    *   **高内聚低耦合**：功能模块边界清晰，代码模块化。
    *   **多维数据持久化**：针对不同数据类型，合理采用多样化的存储策略。

---

## 3. 系统技术架构设计

为确保系统功能的合理性与扩展性，技术架构建议采用经典的分层设计：

```
+-------------------------------------------------------------------+
|                           表示层 (UI Layer)                        |
|   Activity (Login, Main, Edit) | Fragment (Habit, Todo, Pomodoro, Mine) |
|   View (RecyclerView, TabLayout, ViewPager2, Custom Timer View)  |
+-------------------------------------------------------------------+
|                          业务逻辑层 (Logic Layer)                  |
|   Presenter/ViewModel | BroadcastReceiver (强制下线, 每日提醒)      |
|   Service (番茄钟倒计时服务, 音乐播放服务)                         |
+-------------------------------------------------------------------+
|                          数据源与支撑层 (Data Layer)                |
|   SQLite/Room (事务数据) | SharedPreferences (配置信息)             |
|   File Storage (备份/日志) | ContentProvider (跨进程共享)           |
|   Location API (位置服务)  | Local AI Engine (本地推荐算法)         |
+-------------------------------------------------------------------+
```

---

## 4. 功能模块详细需求

应用主要分为四大板块（通过底部导航栏切换）：**今日习惯**、**今日待办**、**番茄钟**、**我的（个人中心）**。

### 4.1 核心板块一：今日习惯 (Today's Habits)

#### 4.1.1 界面交互说明
*   **顶部日期栏**：支持左右滑动或点击切换日期（如“周一 11”、“周二 12”等），默认选中当天。
*   **列表展示**：以卡片形式展示今日需打卡的习惯。习惯卡片包含图标、习惯名称、当前打卡状态（已完成/未完成）。
*   **快速打卡**：点击习惯卡片上的打卡区域，伴随轻微的触觉反馈或简单动画，卡片状态更新为已完成。
*   **添加按钮**：右下角提供悬浮按钮（FAB）或局部添加按钮，点击跳转/弹出习惯创建页面。

#### 4.1.2 关键技术点实现
*   **UI 布局**：采用 `ConstraintLayout`（约束布局）嵌套 `RecyclerView` 实现水平日期栏与纵向习惯列表。
*   **碎片化设计**：通过 `Fragment` 承载本模块，便于在平板或不同分辨率屏幕下进行自适应适配。
*   **数据持久化**：使用 SQLite 数据库存储习惯元数据（名称、周期、频率）；使用 `SharedPreferences` 记录每日打卡状态戳（Key 为 `Date_HabitId`）。

---

### 4.2 核心板块二：今日待办 (Today's To-Dos)

#### 4.2.2 界面交互说明
*   **待办列表**：按时间先后或紧急程度排列的待办事项。支持复选框勾选完成。
*   **滑动操作**：支持对列表项进行左滑删除或右滑编辑。
*   **新增待办**：点击添加按钮，弹出输入框或新 Activity，支持设置任务名称、截止时间与备注。

#### 4.2.3 关键技术点实现
*   **活动间的数据传递**：点击编辑或新增时，通过 `Intent` 携带 `Bundle` 数据在主页面与编辑页面之间进行双向数据传递（使用 `startActivityForResult` 或现代的 `ActivityResultLauncher`）。
*   **列表动画与手势**：结合 `ItemTouchHelper` 实现 `RecyclerView` 的滑动删除（Swipe to dismiss）操作，提升交互体验。

---

### 4.3 核心板块三：番茄钟 (Pomodoro Timer)

#### 4.3.1 界面交互说明
*   **状态展示**：默认显示“开始第一个番茄钟”，提供默认时长选择（如 30 分钟 / 25 分钟）。
*   **计时过程**：点击开始后，界面转为倒计时状态，通过圆形进度条展示剩余时间，背景颜色随之改变（如转为宁静的蓝色或深色）。
*   **控制项**：中途支持暂停、放弃和完成操作。

#### 4.3.2 关键技术点实现
*   **后台服务 (Service)**：使用 `Foreground Service`（前台服务）处理倒计时逻辑，确保应用在进入后台或锁屏时倒计时不被系统强行终止。
*   **多媒体展示**：
    *   计时过程中可选播放白噪音（使用 `MediaPlayer` 读取本地音频或 raw 资源）。
    *   计时结束时，发出振动提示并播放提示音效。
*   **通知栏同步**：在状态栏发送通知，实时更新倒计时进度，支持在通知栏直接点击“暂停”或“放弃”。

---

### 4.4 核心板块四：我的与系统设置 (Mine & Settings)

#### 4.4.1 界面交互说明
*   **个人信息区**：展示用户头像、登录状态（“未登录/已登录”）、会员状态等。
*   **数据统计面板**：以格子的形式简要展示“习惯数”、“待办数”、“专注数（次数或时长）”。
*   **设置列表**：包含数据备份、密码锁、习惯情境管理、每日提醒管理、意见反馈及使用权限说明。

#### 4.4.2 关键技术点实现与模块划分

| 设置子项 | 对应功能描述 | 技术实现路径 |
| :--- | :--- | :--- |
| **数据备份** | 本地数据导出与恢复 | 利用 **文件存储 (File Storage)** 技术，将 SQLite 中的待办与习惯数据导出为本地 `.json` 或 `.db` 备份文件，并支持读取恢复。 |
| **密码锁** | 启动及隐私安全保障 | 使用 **SharedPreferences** 存储密码哈希值与开关状态。开启后，每次进入应用或激活时弹出自定义密码输入 Activity。 |
| **每日提醒管理** | 定时推送提醒打卡 | 结合 `AlarmManager` 与 **广播接收器 (BroadcastReceiver)**，在用户设定时间发送本地通知。 |
| **使用权限与位置** | 基于位置的推荐或记录 | 调用 **基于位置的服务 (LBS)**，获取用户当前的大致经纬度及城市信息，在“今日习惯”中根据地理位置智能提示（如：当检测到在运动场时推荐“跑步”习惯）。 |

---

## 5. 特色与高级功能设计

为了满足课程设计的多样化指标，系统设计了以下具有针对性的进阶技术模块：

### 5.1 强制下线功能 (Force Offline Receiver)
*   **业务场景**：用户多端登录冲突，或用户在设置中主动选择安全退出时，需要强制结束当前会话。
*   **技术实现**：
    *   构建一个基类 `BaseActivity`，在其中注册本地广播接收器（如命名为 `ForceOfflineReceiver`）。
    *   当接收到特定的自定义广播（`com.example.todo.FORCE_OFFLINE`）时，`BaseActivity` 弹出不可取消的系统对话框（AlertDialog），提示“您的账号已在其他设备登录或已失效，请重新登录”。
    *   用户点击确定后，销毁所有 Activity 并跳转至 `LoginActivity`。

### 5.2 跨程序数据共享 (Content Provider)
*   **业务场景**：模拟外部桌面小部件（Widget）或其他辅助应用需要获取今日待办未完成的数量。
*   **技术实现**：
    *   自定义 ContentProvider（如 `TodoProvider`），在 `AndroidManifest.xml` 中进行注册并配置合适的 `authorities`。
    *   重写 `query()`, `insert()`, `update()`, `delete()` 方法，暴露对本地 SQLite 数据库中待办事项表的部分访问权限，供测试程序进行跨应用读写验证。

### 5.3 辅助功能模块（Fragment 简易交互）
*   **业务场景**：提供轻量级的使用问题反馈或简易的社区讨论角。
*   **技术实现**：
    *   在“反馈”或“习惯情境管理”中，通过 Fragment 局部更新实现一个类似聊天室的单页布局。
    *   用户发送反馈内容后，系统通过简易的 Handler 模拟客服的延时回复，展现 Fragment 之间的动态交互。

### 5.4 本地人工智能模块（Local AI Component）
*   **业务场景**：根据用户的历史行为和当前时间段，个性化推荐适合当前进行的习惯（例如：早上推荐“早起喝水”，晚上推荐“阅读”）。
*   **技术实现**：
    *   **基于规则的轻量级决策决策器**（或简单的本地朴素贝叶斯分类算法）：提取输入特征（当前时间戳、当前 LBS 地理位置标签、历史打卡频次），在本地计算各习惯的权重得分。
    *   将得分最高的 1-2 个习惯卡片在“今日习惯”顶部进行高亮或以“智能推荐”标签的形式展现。

---

## 6. 数据结构与持久化设计

应用涉及的数据表结构主要分为三张核心表：用户表、习惯表、待办表。

### 6.1 待办数据表 (todo_item)
| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 ID |
| `title` | TEXT | NOT NULL | 待办名称 |
| `note` | TEXT | | 备注详细内容 |
| `due_date` | LONG | | 截止日期时间戳 |
| `is_completed` | INTEGER | DEFAULT 0 (0/1) | 是否已完成 |
| `priority` | INTEGER | DEFAULT 1 | 优先级 (1-低, 2-中, 3-高) |

### 6.2 习惯数据表 (habit_item)
| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键 ID |
| `name` | TEXT | NOT NULL | 习惯名称 |
| `icon_res` | TEXT | | 图标资源标识符 |
| `frequency` | TEXT | | 频率（如每日、每周特定天） |
| `create_time` | LONG | | 创建时间 |

---

## 7. 非功能性需求与约束

### 7.1 界面规范与自适应
*   **视觉风格**：整体采用 Material Design 风格，配色选用温暖、柔和的马卡龙色调（如米黄色背景），减少高饱和度色彩造成的视觉疲劳。
*   **自适应布局**：使用百分比布局或 `ConstraintLayout` 约束，确保在 16:9、20:9 等不同长宽比的安卓手机屏幕上，界面元素不发生重叠或变形。

### 7.2 安全性与健壮性
*   **权限管理**：在运行至 LBS 服务或多媒体文件读取时，系统必须在运行时动态申请权限，并对用户拒绝权限的情况进行合理降级处理。
*   **数据异常防护**：对数据库的所有读写操作应当在后台线程（如 `AsyncTask`、协程或 `Thread`）中进行，避免造成界面卡死（ANR）。