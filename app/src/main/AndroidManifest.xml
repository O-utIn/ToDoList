<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".ToDoListApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoList">

        <!-- ContentProvider for cross-app todo data sharing -->
        <provider
            android:name=".provider.TodoProvider"
            android:authorities="com.example.todolist.provider"
            android:exported="true" />

        <!-- Splash / Cover Screen -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.ToDoList" />

        <!-- Login Screen -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:label="登录" />
        <!-- Lock Screen -->
        <activity
            android:name=".LockActivity"
            android:exported="false" />

        <!-- Edit Activities -->
        <activity
            android:name=".EditTodoActivity"
            android:exported="false"
            android:label="编辑待办" />
        <activity
            android:name=".EditHabitActivity"
            android:exported="false"
            android:label="编辑习惯" />

        <!-- Pomodoro Activities -->
        <activity
            android:name=".PomodoroTimerActivity"
            android:exported="false"
            android:label="番茄钟计时" />
        <activity
            android:name=".PomodoroSettingsActivity"
            android:exported="false"
            android:label="番茄钟设置" />

        <!-- Pomodoro Foreground Service -->
        <service
            android:name=".service.PomodoroService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Receivers -->
        <receiver
            android:name=".receiver.NotificationActionReceiver"
            android:exported="false" />

        <!-- Home screen widget showing pending todo count and top items -->
        <receiver
            android:name=".widget.TodoWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.todolist.APPWIDGET_MANUAL_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/todo_widget_info" />
        </receiver>
    </application>

</manifest>
