# 三、实验过程和代码与结果

## 1. APP的构建过程及结果

### 1.1 开发环境搭建

本项目基于 Android Studio 2025.1 进行开发，采用以下技术栈：

- **开发语言**：Java（主体）+ Kotlin（测试用例）
- **构建工具**：Gradle 8.x（Kotlin DSL）+ Version Catalog（`libs.versions.toml`）
- **最低 SDK**：API 24 (Android 7.0)
- **目标/编译 SDK**：API 36
- **JDK 版本**：Java 11（启用 `coreLibraryDesugaring` 以支持 `java.time` 包）

### 1.2 项目结构搭建

项目采用经典的三层架构设计（表示层 → 业务逻辑层 → 数据源与支撑层），源代码目录结构如下：

```
app/src/main/java/com/example/todolist/
├── ai/                         # AI 模块（Chat + CMD解析 + 本地推荐）
├── adapter/                    # 5 个 RecyclerView 适配器
├── data/
│   ├── dao/                    # 6 个 Room DAO 接口
│   └── entity/                 # 7 个 Room 实体类
├── fragments/                  # 6 个 Fragment（对应 5 大板块）
├── provider/                   # 1 个 ContentProvider（跨进程共享）
├── receiver/                   # 3 个 BroadcastReceiver
├── service/                    # 1 个前台 Service（番茄钟）
├── util/                       # 8 个工具类
├── widget/                     # 1 个桌面角标辅助类
└── *.java                      # 8 个 Activity
```

### 1.3 构建过程与结果

**（1）依赖配置**

通过 Gradle Version Catalog 统一管理依赖版本（`gradle/libs.versions.toml`），核心依赖包括：

| 依赖库 | 版本 | 用途 |
|:---|:---|:---|
| Room | 2.6.1 | 本地 SQLite 数据库 ORM |
| Retrofit 2 + OkHttp | 2.9.0 / 4.11.0 | DeepSeek AI API 网络通信 |
| Gson | 2.10.1 | JSON 序列化/反序列化 |
| Material 3 | 1.11.0 | Material Design 3 UI 组件 |
| Play Services Location | 21.0.1 | FusedLocation 定位服务 |
| Desugar JDK Libs | 2.1.4 | `java.time` 向后兼容至 API 24 |

**（2）数据库设计**

使用 Room 框架定义 7 张核心数据表：`todo_item`（待办）、`habit_item`（习惯）、`habit_check`（打卡记录）、`habit_checkin`（新版打卡）、`pomodoro_task`（番茄任务）、`pomodoro_session`（番茄会话）、`chat_message`（AI 聊天记录）。所有表均包含 `user_id` 字段实现多用户数据隔离。数据库版本为 v9，采用 `fallbackToDestructiveMigration()` 策略。

**（3）UI 布局开发**

共编写 34 个 XML 布局文件（8 个 Activity 布局、6 个 Fragment 布局、5 个对话框布局、9 个列表项布局、6 个其他布局），以及 25 个 Drawable XML 文件（卡片背景、日历样式、优先级 Chip 样式、矢量导航图标等）。

**（4）构建结果**

执行 `./gradlew assembleDebug` 成功生成 Debug APK，所有 47 个 Java 源文件编译通过，应用可正常安装运行。

---

## 2. APP的功能、关键问题及解决方案

### 2.1 五大核心功能模块

#### （1）今日习惯（HabitFragment）

**功能描述**：支持创建每日/每周/特定日期三种模式的习惯，可设置打卡提醒时间。顶部日历栏支持周/月视图折叠切换，日历格子右上角显示当天待打卡数量角标。习惯卡片包含彩色圆点、名称、频率摘要，点击复选框快速打卡，左滑删除支持 Snackbar 撤销。

**技术实现**：
- 日历组件：`CalendarMonthAdapter` 渲染 42 格（6 行 × 7 列）月历网格，通过 `updateGrid()` 和 `updateCounts()` 两个独立更新方法避免全量刷新导致的闪烁
- 习惯过滤：通过 `HabitItem.isActiveOnDate(LocalDate)` 方法解析 `schedule_config` JSON，判断每日/每周/特定日期模式的习惯是否在选中日期生效
- 滑动删除：使用 `ItemTouchHelper.SimpleCallback` 结合 RecyclerView 实现左滑删除，弹出确认对话框，通过 Snackbar 的 `setAction()` 实现撤销恢复

**关键代码片段（日历角标更新）**：

```java
// CalendarMonthAdapter.java
/** Update badge counts only, preserve grid and selection. */
public void updateCounts(Map<LocalDate, Integer> counts) {
    this.counts = counts;
    notifyDataSetChanged();
}

// onBindViewHolder 中：
Integer count = counts != null ? counts.get(date) : null;
if (count != null && count > 0) {
    tvBadge.setText(String.valueOf(Math.min(count, 99))); // max "99"
    tvBadge.setVisibility(View.VISIBLE);
} else {
    tvBadge.setVisibility(View.GONE);
}
```

#### （2）今日待办（TodoFragment）

**功能描述**：支持创建带标题、备注、截止日期/时间、优先级（低/中/高）的待办事项。列表按"未完成优先 → 截止日期 → 优先级降序"排列。待办卡片左侧显示优先级色带（红=高/橙=中/绿=低），截止日期智能显示（今天/明天/昨天/MM-dd），已完成项灰色删除线显示。

**技术实现**：
- 优先级 UI：三级 Chip 选择器（`chip_selected_bg.xml` / `chip_default_bg.xml`），点击切换背景和文字颜色
- 日期智能显示：`DateUtils.formatDueDate(long)` 方法计算与今日差值，返回"今天"/"明天"/"昨天"或 MM/dd 格式（`util/DateUtils.java:93-102`），在 `TodoAdapter.java:86` 调用渲染到待办卡片
- 数据传递：`TodoFragment.java:103` 通过 Intent `putExtra("todo_id", item.id)` 传递待办 ID → `EditTodoActivity.java:132-134` 通过 `getIntent().getLongExtra("todo_id", -1)` 接收并判断新建/编辑模式

#### （3）番茄钟（PomodoroFragment + PomodoroTimerActivity + PomodoroService）

**功能描述**：支持前台服务驱动的倒计时，三段式自动循环（工作 → 短休息 → 长休息 → 工作）。全屏计时界面含圆形进度条和大字号倒计时数字。通知栏实时显示进度条及暂停/停止操作按钮。支持任务列表管理，预设时长芯片（25/30/45/60 分钟）选中高亮。

**技术实现**：
- 前台服务：`PomodoroService` 继承 `Service`，使用 `CountDownTimer` 1 秒间隔驱动，`START_STICKY` 确保进程死亡后自动恢复
- 广播通信：6 种自定义广播（TICK / PAUSED / RESUMED / STOPPED / PHASE_CHANGED / FINISHED），通过 `setPackage()` 限制包内广播避免 API 26+ 隐式广播限制
- 状态持久化：所有计时状态（running / paused / phase / remaining / total / endTime）写入 SharedPreferences，进程死亡后通过 `restoreState()` 恢复
- 三段式循环：`advancePhase()` 方法判断当前阶段 → 根据配置的周期数选择下一阶段 → 自动开始新阶段倒计时

**关键代码片段（三段式循环切换）**：

```java
// PomodoroService.java
private void advancePhase() {
    SharedPreferences p = getSharedPreferences(PREFS, MODE_PRIVATE);
    if (phase == PHASE_WORK) {
        // 工作完成 → 递增全局计数 → 判断短休或长休
        int count = p.getInt("pomodoro_count", 0);
        p.edit().putInt("pomodoro_count", count + 1).apply();
        workCount++;
        int cycles = p.getInt("cycles_before_long", 4);
        if (workCount % cycles == 0) {
            phase = PHASE_LONG;
            totalMillis = p.getInt("long_break", 15) * 60L * 1000L;
        } else {
            phase = PHASE_SHORT;
            totalMillis = p.getInt("short_break", 5) * 60L * 1000L;
        }
    } else {
        // 休息结束 → 回到工作
        phase = PHASE_WORK;
        totalMillis = p.getInt("pomodoro_length", 25) * 60L * 1000L;
    }
    // ... 发送广播通知UI、更新通知栏、启动新倒计时
}
```

**时长芯片高亮实现**：使用 `chip_selector_bg.xml`（StateListDrawable）定义选中/未选中状态，选中时黄色填充（`#FFD54F`）+ 深黄描边（`#FFC107`），未选中时为浅灰背景。

#### （4）AI 智能助手（ChatFragment + CommandParser + RecommendationEngine）

**功能描述**：气泡式聊天界面，集成 DeepSeek Chat API。用户可用自然语言创建/修改/删除/查询待办、习惯和番茄钟任务（12 种命令）。AI 回复中的命令由本地 `CommandParser` 解析并直接操作 Room 数据库。支持多轮对话、消息持久化、API Key 热切换、清空对话、长按删除单条消息。

**技术实现**：
- DeepSeek API 集成：`ChatClient` 单例（双检锁）管理 Retrofit 客户端，60 秒超时，支持 `resetInstance()` 热切换 API Key
- CMD 协议：系统提示（System Prompt）注入命令格式规范，AI 回复中嵌入 `[CMD]{"action":"...","参数":"值"}[/CMD]` 块，由 `CommandParser.process()` 正则提取并执行
- 命令执行器：`CommandParser` 通过 DAO 层操作 Room 数据库，支持 12 种操作（create_todo/delete_todo/complete_todo/update_todo/list_todos 等）
- 中文星期解析：`parseChineseDays()` 支持"周一、二、四"和"124"两种格式
- 重试机制：若 AI 回复不含命令但用户意图为操作请求，自动重试一次并附加强提醒

**关键代码片段（CMD 协议解析）**：

```java
// CommandParser.java
private static final Pattern CMD_PATTERN =
        Pattern.compile("\\[CMD\\](.*?)\\[\\/CMD\\]", Pattern.DOTALL);

public Result process(String aiText) {
    StringBuilder cleanedText = new StringBuilder(aiText);
    StringBuilder execLog = new StringBuilder();
    Matcher m = CMD_PATTERN.matcher(aiText);
    while (m.find()) {
        String json = m.group(1).trim();
        String outcome = execute(json);  // 执行命令，返回结果文本
        if (outcome != null) execLog.append(outcome).append("\n");
        // 从显示文本中移除命令块
        int start = cleanedText.indexOf(m.group());
        int end = start + m.group().length();
        if (start >= 0) cleanedText.replace(start, end, "");
    }
    String display = cleanedText.toString().trim();
    if (display.isEmpty()) display = execLog.toString().trim();
    return new Result(display, execLog.toString().trim());
}
```

**本地智能推荐引擎**：

```java
// RecommendationEngine.java
// 基于规则的评分系统（无需网络）
//   时间槽匹配（权重 2.0）+ 历史完成率（权重 3.0）+ 今日未打卡奖励（权重 1.0）
public List<HabitItem> recommend(int limit) {
    int currentSlot = getCurrentTimeSlot();             // 早/午/晚
    long todayStamp = DateUtils.toDateStamp(LocalDate.now());
    List<ScoredHabit> scored = new ArrayList<>();
    for (HabitItem habit : allHabits) {
        double score = 0.0;
        // 1) 时间槽匹配
        Integer preferredSlot = TIME_SLOTS.get(habit.name);
        if (preferredSlot == null) preferredSlot = 1;   // 默认下午
        if (preferredSlot == currentSlot) score += 2.0;
        // 2) 近 7 天完成率
        double completionRate = getRecentCompletionRate(habit.id, todayStamp, 7, userId);
        score += completionRate * 3.0;
        // 3) 今日未打卡奖励
        HabitCheck todayCheck = db.habitCheckDao().getByHabitAndDate(habit.id, todayStamp, userId);
        if (todayCheck == null) score += 1.0;
        scored.add(new ScoredHabit(habit, score));
    }
    scored.sort((a, b) -> Double.compare(b.score, a.score));
    // 返回 Top-N
}
```

#### （5）我的（MineFragment）

**功能描述**：个人中心包含用户登录/注册、40 款 emoji 头像选择、数据统计面板（习惯数/待办数/番茄专注完成次数）、位置卡片（后台自动定位 + LiveData 驱动刷新）、设置列表（AI 配置、数据备份管理、应用权限入口）。

**关键技术点**：
- **密码安全**：登录密码 SHA-256 哈希存储（`HashUtils.java`）；"记住密码"功能使用 Android Keystore 硬件级 AES-256/GCM 加密（`CryptoUtils.java`），密钥不离开设备，卸载后自动销毁
- **数据备份**：`BackupManager.java` 将所有待办和习惯导出为 JSON，双存储策略（应用私有目录 `files/backups/` + 公共 Downloads 文件夹）
- **后台自动定位**：`MainActivity` 启动时自动调用 `requestSingleLocation()`，15 秒超时自动降级到上次已知位置，`LocationHelper` 通过 LiveData 广播结果，`MineFragment` 观察并即时显示

### 2.2 其他特色功能

#### （1）启动页励志语录（SplashActivity）

应用冷启动展示 2.5 秒过渡动画（600ms 淡入效果）。优先从 `api.quotable.io` 获取英文励志语录（OkHttp 同步请求，5 秒超时），网络失败则从本地预置的 15 条中文名言中随机选取。

#### （2）强制下线功能（ForceOfflineReceiver）

用于多端登录冲突场景。`ForceOfflineReceiver` 在 `MainActivity` 中动态注册，监听自定义广播 `FORCE_OFFLINE`。收到广播后清除 `app_unlocked` 标记，弹出不可取消的 AlertDialog 提示用户重新登录。

#### （3）ContentProvider 跨进程数据共享（TodoProvider）

Authority: `com.example.todolist.provider`，提供 4 个 URI 路径：`/todos`（所有待办）、`/todos/count`（总数）、`/todos/pending`（未完成）、`/todos/pending/count`（未完成数量）。返回 `MatrixCursor`，供桌面小部件 (`BadgeHelper`) 查询待办数量并设置桌面角标。

#### （4）每日提醒（DailyReminderReceiver）

结合 `AlarmManager` 在用户设定时间发送本地通知。异步查询 `RecommendationEngine` 获取最适合当前时段的习惯，将习惯名称纳入通知文本。

### 2.3 开发过程中遇到的关键问题及解决方案

#### 问题一：AI 多轮对话中命令丢失

**现象**：用户向 AI 助手发送"创建待办买菜"等操作指令后，AI 有时只回复自然语言描述而不包含 `[CMD]` 命令块，导致操作无法被 `CommandParser` 解析执行。

**原因分析**：DeepSeek 大模型在多轮对话中可能"忘记"系统提示中的命令格式要求，尤其在对话历史增长后，模型倾向于只回复自然语言。

**解决方案**：
1. **强化系统提示**：将 CMD 协议说明放在系统提示的 `🔴 核心规则` 位置，设置最高优先级，并增加具体示例
2. **自动重试机制**：`ChatFragment.java` 中实现 `userLikelyWantedAction()` 方法，检测用户消息中是否包含"创建/删除/完成/修改/添加/番茄/习惯/待办"等关键词。若 AI 回复不含命令但用户意图明确为操作请求，自动在对话上下文中追加强制提醒消息（"你刚才的回复没有包含 [CMD] 命令块！用户要求执行操作，你必须严格按格式发出命令。现在请仅回复命令（不要其他文字）："），并调用 `retryCommand()` 重新请求 API
3. **上下文保持**：将完整 AI 回复（含 CMD 标签）存回 `conversation` 列表，避免模型因无法看到自己之前发过的命令格式而退化

**经验总结**：提示工程（Prompt Engineering）在实际应用中的鲁棒性至关重要。不能完全依赖单次 API 调用返回正确格式，必须在本地实现兜底+重试机制。

#### 问题二：番茄钟进程死亡后状态丢失

**现象**：用户切换应用或锁屏后，系统可能因内存压力杀死 `PomodoroService` 进程，导致番茄钟倒计时中断、通知栏消失。

**解决方案**：
1. 采用 `START_STICKY` 返回策略，系统在资源恢复后自动重建 Service
2. 所有计时状态（running / paused / phase / total / remaining / endTime）实时写入 SharedPreferences
3. 在 `onStartCommand()` 中调用 `restoreState()` 恢复状态，通过 `endTime - now` 计算真实剩余时间
4. 若进程死亡期间计时器已到期（`endTime <= now`），自动调用 `advancePhase()` 推进到下一阶段

#### 问题三：日历组件计数角标显示错误

**现象**：切换月份或周视图时，日历格子上的计数角标偶尔与实际情况不一致。

**原因分析**：`notifyDataSetChanged()` 刷新时，`counts` 数据与 `days` 网格数据异步获取，存在时序竞争。

**解决方案**：将 `CalendarMonthAdapter` 的更新方法拆分为 `updateGrid(List<LocalDate>, LocalDate)`（更新网格+选中状态）和 `updateCounts(Map<LocalDate, Integer>)`（仅更新角标计数）。两个方法独立调用，避免数据准备不完整时的全量刷新。`HabitFragment` 和 `TodoFragment` 中先设置网格再异步加载计数数据，加载完成后单独刷新角标。

#### 问题四：每周习惯的星期参数解析

**现象**：AI 命令创建"每周一、三、五"习惯时，系统只存储"周一三五"的文本描述，无法正确解析为数字格式 "135"。

**解决方案**：在 `CommandParser.parseDays()` 方法中实现双格式兼容：
- 数字格式：直接过滤非 1-7 字符，保留纯数字串（如 "135"）
- 中文格式：`parseChineseDays()` 方法遍历 21 种中文星期表达（"周一"/"星期一"/"周1"等），检测文本中的匹配模式，映射为对应数字
- 双重后备：若 `days` 参数为空，回退到扫描 `desc` 描述字段中的中文星期名称

#### 问题五：定位请求长时间无响应

**现象**：在某些设备或室内环境下，GPS 定位请求长时间无返回，导致"我的"页面一直显示"正在定位..."。

**解决方案**：在 `LocationHelper.requestSingleLocation()` 中实现 15 秒超时机制：
```java
final Runnable timeoutRunnable = () -> {
    stopTracking();
    getLastLocation(context);  // 降级到上次已知位置
    locationLiveData.postValue(LocationInfo.status("定位超时，显示上次缓存位置"));
};
timeoutHandler.postDelayed(timeoutRunnable, 15_000);
```
同时在 `MainActivity.onCreate()` 中启动定位，利用应用冷启动到用户切换到"我的"页面的时间窗口，使得切换到"我的"时定位通常已完成。

#### 问题六：统计数据面板只显示第一项（习惯数）

**现象**：在"我的"页面中，数据统计面板水平排列三个统计项（习惯数、待办数、番茄专注完成次数），但实际运行时只有第一项（习惯数）可见，待办数和番茄专注数被挤出屏幕。

**原因分析**：统计面板使用 `horizontal` 方向的 `LinearLayout`，三个子项通过 `<include>` 引入 `item_stat.xml` 布局。`item_stat.xml` 根布局的 `android:layout_width` 设置为 `match_parent`，在水平线性布局中第一个子项即占满全部可用宽度，导致后续两个子项被推到屏幕右侧不可见区域。`<include>` 标签未覆盖 `layout_width` 和 `layout_height` 属性，且根据 Android 规则，覆盖 `layout_width` 必须同时覆盖 `layout_height`，否则覆盖无效。

**解决方案**：在每个 `<include>` 标签上同时添加 `android:layout_width="0dp"`、`android:layout_height="wrap_content"` 和 `android:layout_weight="1"` 三个属性，使三个统计项均分面板宽度。

**修改前（`fragment_mine.xml`）**：
```xml
<include layout="@layout/item_stat"
    android:id="@+id/stat_habits" />
<!-- 缺少 layout_width/layout_height/layout_weight，默认继承 item_stat.xml 中的 match_parent -->
<include layout="@layout/item_stat"
    android:id="@+id/stat_todos" />
<include layout="@layout/item_stat"
    android:id="@+id/stat_pomodoros" />
```

**修改后**：
```xml
<include layout="@layout/item_stat"
    android:id="@+id/stat_habits"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1" />

<include layout="@layout/item_stat"
    android:id="@+id/stat_todos"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1" />

<include layout="@layout/item_stat"
    android:id="@+id/stat_pomodoros"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1" />
```

**经验总结**：在 Android 中使用 `<include>` 标签引入布局时，需特别注意 `layout_width` 和 `layout_height` 属性的继承问题。当被引入布局定义了 `match_parent` 的宽度，而外层容器为水平 LinearLayout 时，若不显式覆盖并配合 `layout_weight` 使用，会导致子项无法均匀分配空间。此外，Android 的 `<include>` 规则要求：如果要覆盖 `layout_width`，必须同时覆盖 `layout_height`（反之亦然），否则两条覆盖均不生效。这是一个容易被忽略的陷阱。

---

# 四、实验总结

## 1. 实验过程回顾

本项目从零开始构建了一款完整的 Android 时间管理应用，涵盖了移动互联网应用开发的核心技术栈。开发过程经历了以下阶段：

1. **需求分析与架构设计**（约 1 天）：撰写 PRD 文档，确定五大功能模块、技术架构三层设计、数据库七表结构
2. **基础框架搭建**（约 1 天）：配置 Gradle 构建系统、Room 数据库、导航结构、主题样式
3. **核心功能开发**（约 3 天）：依次实现待办 CRUD、习惯打卡、番茄钟前台服务、AI 聊天助手、个人中心
4. **特色功能完善**（约 2 天）：CMD 命令协议、推荐引擎、定位服务、备份管理、密码锁、强制下线
5. **调试与问题修复**（约 2 天）：解决 AI 命令丢失、日历角标错乱、番茄钟状态恢复、定位超时、统计面板布局等问题
6. **测试与优化**（约 1 天）：多设备适配测试、性能优化、代码审查

## 2. 遇到的错误与解决方法

除了前述六个关键问题外，在开发过程中还遇到了以下典型错误：

**错误一：Room 数据库版本升级导致崩溃**

在迭代开发中频繁修改 Entity 字段定义（如新增 `user_id` 字段、调整 `HabitCheck` 表结构），导致 App 升级安装后因数据库 schema 不匹而崩溃。

**解决方法**：采用 `fallbackToDestructiveMigration()` 策略，并在备份恢复功能中保存/还原数据。每次数据库变更后使用 `BackupManager` 导出测试数据，卸载重装后一键恢复。对于生产环境，后续应考虑实现 `Migration` 对象进行平滑升级。

**错误二：Android 8.0+ 隐式广播限制导致番茄钟 UI 不更新**

在最初版本中，`PomodoroService` 直接调用 `sendBroadcast(intent)` 发送隐式广播，在 Android 8.0+ 设备上 `PomodoroFragment` 和 `PomodoroTimerActivity` 无法接收到 TICK 广播，UI 倒计时不更新。

**解决方法**：在 `sendLocalBroadcast()` 方法中调用 `intent.setPackage(getPackageName())` 将隐式广播转换为显式广播，绕过系统限制。

**错误三：`java.time.LocalDate.now()` 在部分设备上崩溃**

在低版本 Android 设备上（API 24-25），`java.time.LocalDate.now()` 偶尔抛出 `DateTimeException`。

**解决方法**：将所有 `LocalDate.now()` 调用包裹 try-catch，catch 块中使用 `System.currentTimeMillis()` + `Calendar` 作为后备方案计算当前日期。同时启用 `coreLibraryDesugaring` 提供更稳定的 `java.time` 实现。

**错误四：Geocoder 在某些设备上返回空**

在无 Google Play Services 的国产设备上（如部分华为、小米机型），`Geocoder.getFromLocation()` 始终返回空列表或直接抛异常。

**解决方法**：在 `reverseGeocode()` 方法中，先检查 `Geocoder.isPresent()`，不可用时直接返回坐标字符串 `"%.5f, %.5f"`。所有 Geocoder 调用包裹 try-catch，异常时降级为坐标显示。

## 3. 亮点功能代码对应关系

| 序号 | 亮点功能 | 对应核心代码文件 | 技术要点 |
|:---|:---|:---|:---|
| 1 | **AI 自然语言操作（CMD 协议）** | `ai/CommandParser.java:42-67`（正则提取+执行）<br>`fragments/ChatFragment.java:48-93`（系统提示注入）<br>`fragments/ChatFragment.java:339-354`（命令丢失重试） | Pattern 正则匹配 `[CMD]...[/CMD]` 块 → Gson 解析 JSON → DAO 执行数据库操作 → 返回执行日志 |
| 2 | **本地智能推荐引擎** | `ai/recommendation/RecommendationEngine.java:50-99`（评分算法）<br>`fragments/HabitFragment.java`（推荐结果接入 UI） | 三因素加权评分（时间槽2.0 + 完成率3.0 + 未打卡1.0），Top-N 排序 |
| 3 | **番茄钟前台服务三段式循环** | `service/PomodoroService.java:199-237`（`advancePhase()`）<br>`service/PomodoroService.java:252-275`（`restoreState()` 状态恢复） | CountDownTimer 1s 驱动 + SharedPreferences 持久化 + 6 种自定义显式广播 |
| 4 | **AES-256/GCM 硬件级密码加密** | `util/CryptoUtils.java:31-55`（encrypt）<br>`util/CryptoUtils.java:61-88`（decrypt）<br>`util/CryptoUtils.java:93-114`（Keystore 密钥生成） | Android Keystore `KeyGenParameterSpec` 256 位 AES/GCM，IV+密文 Base64 编码，密钥不离开设备 |
| 5 | **日历组件角标计数** | `adapter/CalendarMonthAdapter.java:41-52`（独立更新方法）<br>`adapter/CalendarMonthAdapter.java:100-106`（角标渲染） | `updateGrid()` 与 `updateCounts()` 分离更新，避免全量刷新闪烁 |
| 6 | **后台自动定位 + 超时降级** | `util/LocationHelper.java:93-182`（单次定位+超时）<br>`util/LocationHelper.java:247-274`（反向地理编码） | FusedLocationProviderClient + 15s Handler 超时 → `getLastLocation()` 降级 |
| 7 | **数据备份双存储** | `util/BackupManager.java:42-91`（导出 JSON）<br>`util/BackupManager.java:97-138`（公共 Downloads 写入） | Room 全量导出 JSON + API 29+ MediaStore / API <29 直接文件 IO |
| 8 | **启动页励志语录** | `SplashActivity.java:85-118`（网络+本地双语料） | OkHttp 5s 超时 → 本地 15 条中文名言后备 + 600ms 淡入动画 |
| 9 | **ContentProvider 跨进程共享** | `provider/TodoProvider.java`（4 条 URI 路径）<br>`widget/BadgeHelper.java`（桌面角标消费端） | MatrixCursor 构造结果集 + 桌面通知 `setNumber()` 设置角标 |
| 10 | **多用户数据隔离** | `data/entity/*.java`（所有实体含 `user_id`）<br>`data/dao/*.java`（查询按 `user_id` 过滤） | 所有 DAO 查询兼容新旧数据（`user_id=''` 或匹配当前用户） |

## 4. 不足之处与改进方向

1. **AI 对话体验**：当前命令协议需完全依赖 AI 生成正确 JSON，对复杂意图（如"把昨天的未完成待办全部移到今天"）支持不足。后续可引入 Function Calling 机制，由本地定义函数 schema 让模型直接调用，提高可靠性。

2. **数据库迁移**：当前使用 `fallbackToDestructiveMigration()` 简单粗暴，升级时数据丢失。后续应实现 Room `Migration` 对象进行平滑升级。

3. **同步与云端能力**：当前所有数据仅存储在本地，无云端同步功能。后续可接入 Firebase Firestore 或自建后端实现多设备数据同步。

4. **测试覆盖**：当前仅有 2 个占位测试文件，缺乏单元测试和 UI 测试。后续应对 CommandParser、RecommendationEngine、BackupManager 等核心逻辑类补充 JUnit 测试。

## 5. 学习收获与体会

通过本次《移动互联网应用》课程设计项目的完整开发实践，我获得了以下几点重要收获：

**（1）Android 组件化开发的系统性理解**。本项目涉及了 Activity、Fragment、Service、BroadcastReceiver、ContentProvider 四大组件以及 Room、SharedPreferences、Android Keystore 等多种持久化方式。通过实际编码，我深刻理解了各组件的生命周期、通信方式（Intent/Broadcast/LiveData）以及适用场景。

**（2）前后台协同与进程保活**。番茄钟功能要求应用在后台甚至进程被杀死后仍能正确计时，这促使我深入研究了 Android 前台服务机制、`START_STICKY` 策略以及 SharedPreferences 状态持久化的组合方案。

**（3）提示工程与大模型集成实践**。AI 助手功能的开发让我认识到，将大模型集成到实际产品中不仅仅是调用 API，还需要设计鲁棒的协议（CMD）、考虑失败重试、处理格式异常等工程问题。**在实际应用中，永远不能完全信任 AI 响应的正确性，必须在本地实现校验和兜底机制。**

**（4）防御性编程的重要性**。Android 生态碎片化严重，不同设备、不同 Android 版本的 API 行为可能完全不同。本项目中的定位降级、Geocoder 兼容处理、`LocalDate.now()` 异常保护等细节，都是防御性编程的具体体现。

**（5）软件工程的全流程视角**。从 PRD 撰写、架构设计、编码实现到问题排查，完成了一次完整的软件开发流程，加深了对"需求→设计→实现→测试"这一软件工程核心循环的理解。

**（6）安全意识的建立**。在实现登录和数据保护功能时，学习了 SHA-256 哈希、AES-256/GCM 对称加密、Android Keystore 硬件安全模块等实用安全技术，意识到移动端数据安全不仅仅是"加密"二字，更需要合理的密钥管理和安全存储方案。

**（7）Android 布局细节的深入理解**。在修复统计面板 `<include>` 宽度问题时，深入理解了 Android `<include>` 标签的属性覆盖规则：覆盖 `layout_width` 必须同时覆盖 `layout_height`，否则二者均保持被引入布局的原始值。这类细节在框架文档中容易被忽略，却能在实际开发中造成"代码正确但显示异常"的困惑，是只有通过实践才能积累的宝贵经验。
